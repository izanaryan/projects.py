import math

def prime_num_check():
    is_prime=True 
    for i in range(2,number):
        if number%i==0:
            is_prime=False
    if is_prime:
        print("number is prime.")
    else:
        print("It\'s is not prime")

number=int(input("Enter number:"))            
prime_num_check()
